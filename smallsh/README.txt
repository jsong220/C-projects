To compile and run:

gcc -o smallsh smallsh.c 
chmod +x smallsh

mini shell with basic capabilities. 

Built in cmds:
exit and cd.
Also handles background and foreground processes. 

Other cmds handled with exec() command.
